QuaVaProt DB MutaQuant edition docker app

To run the app, first download the data.zip file and extract to the QuaVaProt_db folder, in a folder titled 'data' Download link: https://figshare.com/s/56569878c75622e5f849

Then build and run the docker image using the command: docker-compose up --build
